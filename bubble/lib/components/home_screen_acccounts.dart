class HomeAccounts {
  String? username;
  String? message;
  dynamic clock;
  String? image;
  int? messageNumber;
  HomeAccounts(
      {this.username,
      this.message,
      this.clock,
      this.image,
      this.messageNumber});
}
